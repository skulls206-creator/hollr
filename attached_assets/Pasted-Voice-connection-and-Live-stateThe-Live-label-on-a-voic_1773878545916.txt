Voice connection and “Live” stateThe “Live” label on a voice channel should only appear when:At least one user is actively connected to the voice channel audio stream.The client has a successful voice WebRTC/WS connection established to your voice server for that channel.�When 2+ users join the same voice channel:Each client must subscribe to the others’ audio tracks and mix them into the local output stream.If the AI cannot actually test media devices, it should:Implement all signaling (join/leave events, SDP offer/answer, ICE, mute state).Stub or log audio send/receive events so you can verify in the browser.On your side you will:Join from two separate browser sessions and confirm that:You see both users listed in the voice UI.Audio from each user is audible to the other when their mic is unmuted.Show every user in the voice channelUnder each voice channel in the left sidebar, render a small list of connected users:When the channel is empty: show just the channel name (no users).When at least one user joins:The channel row displays a “Live” label.Directly underneath, render one row per connected user with:User avatar.Username.Current mic status (muted/unmuted icon).This list should update in real time:On voiceUserJoined(channelId, user) events: append that user to the list for that channel.On voiceUserLeft(channelId, userId) events: remove them.On voiceUserUpdated events (mute, volume, speaking): update the corresponding entry.Speaking indicator (green circle around avatar)Each user in a voice channel gets a speaking indicator:When the backend/RTC layer detects the user’s outgoing audio level above a threshold:Emit a voiceSpeakingStart(channelId, userId) event to all clients.When the audio level falls below that threshold for some debounce timeout:Emit voiceSpeakingStop(channelId, userId).In the UI:Render a circular avatar for each user.When speaking is true for that user:Draw a glowing green ring around the avatar (outer stroke, maybe soft box-shadow).�When speaking is false:Hide the ring.Use a simple audio-level rule:On the client that is sending audio:Monitor the microphone input’s RMS/peak value.When above threshold, set local speaking state and notify the server.Do not rely on playback volume for the speaking state; it must reflect the speaker’s mic, not the listener’s output.�Bottom bar controls (profile, mic, headset, settings)Make the bottom bar buttons fully functional:Profile buttonClicking the profile button opens the current user’s profile panel:Display:Avatar and display name.Basic info (email mask, username, ID).Current presence status and custom status (if you have it).Provide quick actions:Change avatar.Change display name.Set presence (Online / Idle / DND / Invisible).This can be implemented as:A right-side drawer, ORA centered modal card.Mic buttonToggles local mute/unmute for the microphone:When muted:Do not send audio frames to the voice server.Emit a voiceUserMuted(channelId, userId, true) event to others.Update the UI:Mic icon with a slash.User’s avatar shows a small mic-off badge.When unmuted:Resume sending audio.Emit voiceUserMuted(channelId, userId, false).Maintain sync between:UI state (button icon).Voice pipeline state (actual mic capture on/off).Server state (for others to show your mute icon correctly).�Headset buttonToggles deafen/undeafen:When deafened:Mute all incoming audio from the voice mix.Optionally also self-mute (Discord-style).Show a headset-with-slash icon.When undeafened:Resume playback of all remote user audio streams.Emit voiceUserDeafened(channelId, userId, bool) so others can see the headset icon if you want.Settings buttonOpens voice settings:Input device selection.Output device selection.Input volume and automatic gain / input sensitivity (with live meter).Push-to-talk vs voice-activity mode.The AI should wire this to actual media device enumeration (via navigator.mediaDevices.enumerateDevices or equivalent) and persist selections in user settings.�Per-user volume control in voice channelIn the voice user list within a channel, make each username or avatar clickable to open a small context menu:On click:Open a menu anchored to that user.Include a “Volume” slider from 0% to 200% (or 0–100). This controls how loud that person is for the current client only.�Behavior:The slider adjusts only the local playback gain for that remote user’s audio stream.It does NOT change how loud they are for other participants.Volume values should be stored per-channel per-user on this client so that if you rejoin, your custom settings are restored.Implementation details:Each remote user’s audio should have an individual gain node (Web Audio API) or equivalent volume control.Moving the slider updates the gain node in real time.A value of 0% effectively mutes that user locally.Optional extras:Add a “Mute user” checkbox next to the slider to hard-mute them locally.